v1.6.3 让不同时间范围下的时间轴始终保持规整等距。

## 更新内容

- 时间轴直接根据所选的 1、3、6 或 24 小时时间范围生成，不再按实际探测时间插值刻度位置。
- 根据时间跨度和绘图区宽度自动选择整 10 分钟倍数的间隔，避免标签重叠。
- 所有普通时间刻度与右端“现在”标记保持等距，不受缺测、补测或探测频率变化影响。
- 修正 Windows 干净构建时的单文件运行库清单，确保打包后的 EXE 可直接启动。
